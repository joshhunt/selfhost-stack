prometheus.scrape "traefik" {
  // Read from
  targets = [{
    __scheme__ = "https",
    __address__ = "traefik.trtr.co:443",
    "job" = "traefik",
    "project" = "selfhost-stack",
    "service" = "traefik",
  }]

  // Write to
  forward_to = [publish.metrics_for_host]
}

